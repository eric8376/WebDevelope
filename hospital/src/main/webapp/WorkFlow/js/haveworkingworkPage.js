// 布局模型
var viewport = new Ext.Viewport({
    layout: 'fit',
    items: [haveworkingworkgrid]/*,
     listeners : {
     resize : function(viewport, adjWidth, adjHeight, rawWidth,
     rawHeight) {
     haveworkingworkgrid.setWidth(document.body.clientWidth);
     haveworkingworkgrid.doLayout();
     Ext.getCmp("haveworkingwork")
     .setWidth(document.body.clientWidth);
     Ext.getCmp("haveworkingwork")
     .setHeight((document.body.clientHeight));

     }
     }*/
});
